import { Box, Typography } from '@mui/material'

function Login() {
  return (
    <Box
      sx={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        backgroundColor: '#f5f5f5',
      }}
    >
      <Typography variant="h4">
        Sistema CAD - Login (En desarrollo)
      </Typography>
    </Box>
  )
}

export default Login
